<header class="w-full flex items-center justify-between bg-green-800 text-white py-4 px-8 shadow-md">
    <h1 class="text-2xl font-bold">Order History</h1>
    <nav class="flex items-center space-x-4">
        <!-- View Orders -->
        <a href="/order/view" class="flex items-center gap-2 px-4 py-2 rounded-md hover:bg-green-700" aria-label="View Orders">
            📋 View Orders
        </a>
        <a href="/order/kitchen" class="flex items-center gap-2 px-4 py-2 rounded-md hover:bg-green-700" aria-label="Kitchen">
            🍳 Kitchen
        </a>
        <!-- New Order -->
        <a id="newOrderButton" class="flex items-center gap-2 px-4 py-2 rounded-md hover:bg-green-700 transition duration-300" aria-label="New Order">
            ➕ New Order
        </a>
    </nav>
</header>

<script>
    // Function to generate a unique Order ID
    function generateOrderId() {
        const prefix = "ORD";
        const timestamp = Date.now(); // Use timestamp to ensure uniqueness
        const randomPart = Math.floor(1000 + Math.random() * 9000); // 4-digit random number
        return `${prefix}${timestamp}${randomPart}`;
    }

    // Dynamically set the URL for the New Order button
    function setOrderButtonUrl() {
        const newOrderButton = document.getElementById("newOrderButton");
        if (newOrderButton) {
            const orderId = generateOrderId();
            newOrderButton.href = `/order/take/${orderId}`;
        } else {
            console.warn("New Order button not found.");
        }
    }

    // Set the button URL when the page loads
    window.addEventListener('load', setOrderButtonUrl);
</script>
