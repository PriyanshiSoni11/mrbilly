<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mr. Billy Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

  <% if(error.length>0){ %>
    <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
        <span class="inline-block mt-1 mb-1 text-white">
            <%= error %>
        </span>
    </div>
    <% } %>

  <div class="flex h-screen">
    <!-- Left Section -->
    <div class="w-1/2 bg-green-800 text-white flex flex-col justify-center items-center p-8">
      <img src="/images/logo.png" alt="Mr. Billy Logo" class="w-48 h-auto">
      <h1 class="text-3xl font-semibold mt-8">Welcome!</h1>
      <p class="mt-2 text-lg">New Here? Create your account.</p>
      <a href="/register" class="mt-6">
        <button class="bg-white text-green-800 px-6 py-2 rounded-lg text-lg hover:bg-green-100 transition duration-300">Sign Up</button>
      </a>
    </div>

    <!-- Right Section (Login Form) -->
    <div class="w-1/2 flex justify-center items-center bg-white p-8">
      <div class="w-full max-w-md">
        <h2 class="text-2xl font-semibold text-green-800 text-center mb-8">Login</h2>
        <form action="/login" method="POST" class="space-y-6">
          <div>
            <label for="email" class="block text-gray-700 text-sm font-medium">Username/Email address *</label>
            <input type="email" id="email" name="email" required class="w-full p-4 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:outline-none mt-2">
          </div>
          <div>
            <label for="password" class="block text-gray-700 text-sm font-medium">Password *</label>
            <input type="password" id="password" name="password" required class="w-full p-4 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:outline-none mt-2">
          </div>
          <div>
            <button type="submit" class="w-full bg-green-800 text-white py-3 rounded-lg text-lg hover:bg-green-700 transition duration-300">Sign In</button>
          </div>
        </form>
        <div class="text-center mt-4">
          <a href="/" class="text-green-800 text-sm">Forgot password?</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
