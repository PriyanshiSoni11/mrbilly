<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mr. Billy - Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 ">

  <%- include('partials/headerForProfile') %>
    <% if(success.length>0){ %>
      <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
        <span class="inline-block mt-1 mb-1 text-white">
          <%= success %>
        </span>
      </div>
      <% } %>
        <div class="max-w-4xl mx-auto mt-10 bg-white shadow-lg rounded-lg p-8">
          <!-- Profile Section -->
          <div class="flex flex-col items-center text-center">
            <img src="/images/defaultProfile.png" alt="Profile Picture" class="w-32 h-32 rounded-full shadow-md mb-4">
            <h1 class="text-3xl font-bold text-green-800">
              <%= user.fullname %>, Proud owner of "<%= user.restaurantname %>" 🥳
            </h1>
          </div>

          <!-- Add Menu Button -->
          <div class="mt-6 flex justify-center">
            <a href="/menu/add"
              class="flex items-center bg-green-800 text-white py-2 px-4 rounded-lg hover:bg-green-700 transition duration-300">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24"
                stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
              </svg>
              Add Your First Menu
            </a>
          </div>
        </div>

        <%- include('partials/footer') %>
</body>

</html>