<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mr. Billy - Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 ">

  <%- include('partials/headerForProfile') %>

  <% if(success.length > 0) { %>
  <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-blue-500">
    <span class="inline-block mt-1 mb-1 text-white">
      <%= success %>
    </span>
  </div>
  <% } %>

  <div class="max-w-4xl mx-auto mt-10 bg-white shadow-lg rounded-lg p-8">
    <!-- Profile Section -->
    <div class="flex flex-col items-center text-center">
      <img src="/images/defaultProfile.png" alt="Profile Picture" class="w-32 h-32 rounded-full shadow-md mb-4">
      <h1 class="text-3xl font-bold text-green-800 mb-2">
        <%= user.fullname %>, Proud owner of "<%= user.restaurantname %>" 🥳
      </h1>
      <p class="text-gray-600 text-lg">"Empowering food experiences one table at a time!"</p>
    </div>

    <!-- Details Section -->
    <div class="mt-8">
      <h2 class="text-2xl font-semibold text-green-800 mb-4">Owner Details:</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
        <div>
          <h3 class="text-lg font-medium text-gray-700">Full Name:</h3>
          <p class="text-gray-600"><%= user.fullname %></p>
        </div>
        <div>
          <h3 class="text-lg font-medium text-gray-700">Email:</h3>
          <p class="text-gray-600"><%= user.email %></p>
        </div>
        <div>
          <h3 class="text-lg font-medium text-gray-700">Address:</h3>
          <p class="text-gray-600"><%= user.address %></p>
        </div>
      </div>

      <h2 class="text-2xl font-semibold text-green-800 mt-8 mb-4">Restaurant Details:</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
        <div>
          <h3 class="text-lg font-medium text-gray-700">Restaurant Name:</h3>
          <p class="text-gray-600"><%= user.restaurantname %></p>
        </div>
        <div>
          <h3 class="text-lg font-medium text-gray-700">Restaurant Address:</h3>
          <p class="text-gray-600"><%= user.restaurantaddress %></p>
        </div>
        <div>
          <h3 class="text-lg font-medium text-gray-700">GSTIN:</h3>
          <p class="text-gray-600"><%= user.gstin || 'Not Provided' %></p>
        </div>
        <div>
          <h3 class="text-lg font-medium text-gray-700">Table Count:</h3>
          <p class="text-gray-600"><%= user.tablecount || 'Not Provided' %></p>
        </div>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>

</body>

</html>
